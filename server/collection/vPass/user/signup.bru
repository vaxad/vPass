meta {
  name: signup
  type: http
  seq: 1
}

post {
  url: {{url}}/user/signup
  body: json
  auth: none
}

body:json {
  {
    "email": "v5@gmail.com",
    "password": "varad123",
    "username": "varad"
  }
}

tests {
  const responseBody = res.getBody(); 
  const newValue = responseBody.token; 
  if(!newValue)return
  bru.setEnvVar("token", newValue);
  
}
